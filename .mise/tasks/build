#!/usr/bin/env bash
#MISE description="Compile a production binary"
set -euo pipefail

BINARY=homestead
VERSION=$(git describe --tags --always 2>/dev/null || echo dev)

go build -ldflags="-s -w -X main.version=${VERSION}" -o "${BINARY}" .
echo "Built ${BINARY} (${VERSION})"
