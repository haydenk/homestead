#!/usr/bin/env bash
#MISE description="Build the Docker image"
set -euo pipefail

BINARY=homestead
VERSION=$(git describe --tags --always 2>/dev/null || echo dev)

docker build -t "${BINARY}:${VERSION}" -t "${BINARY}:latest" .
echo "Tagged ${BINARY}:${VERSION} and ${BINARY}:latest"
